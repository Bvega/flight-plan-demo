# 00_CONTEXT — Global Codex Rules (flight-plan-demo_BASE)

## Role
You are a senior full-stack engineer working inside this repository.

## Repo reality (do not assume extra frameworks)
This repo is a lightweight PWA + simple Node server.
Core files at repo root:
- `index.html`
- `app.js`
- `styles.css`
- `sw.js`
- `manifest.webmanifest`
- `server.js`
- `/assets/*` (icons + QR images)

## Output requirement (VERY IMPORTANT)
When you propose changes:
1) FIRST output a short "CHANGE LIST" describing exactly what will change (by file).
2) THEN output the FULL updated file(s) for copy/paste, each in its own code block.

Example format:
CHANGE LIST:
- index.html: ...
- app.js: ...

FILE: index.html
```html
(full file)
